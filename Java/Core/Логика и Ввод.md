# Сердце программы: Ввод, Ветвление и Циклы

**Проект: Dark Dungeon**

> **Минимальная версия Java: 14+** (используем `switch` expression). Рекомендуем Java 17 LTS или новее.

---

## 0. Где мы и куда идём

В прошлый раз мы создали мир — объявили переменные героя и подземелья. Но мир был мёртвый: ни выбора, ни действия. Переменные — это декорации. Сегодня мы запускаем спектакль.

К концу статьи наша консоль покажет:

text

```
=== DARK DUNGEON ===
Как тебя зовут, странник? > Артас
Выбери класс (1-Маг, 2-Воин, 3-Лучник): > 2
Привет, Артас! Твой класс: Воин | HP: 100

------------------------------
Артас | HP: 100 (Здоров)
Перед тобой стоит Гоблин!
Что делаешь? 1-Атаковать 2-Бежать 3-Выход > 1
Ты наносишь мощный удар! Гоблин повержен!

------------------------------
Артас | HP: 100 (Здоров)
Перед тобой стоит Скелет!
Что делаешь? 1-Атаковать 2-Бежать 3-Выход > 2
Ты убегаешь обратно к развилке...

------------------------------
Артас | HP: 100 (Здоров)
Перед тобой стоит Тролль!
Что делаешь? 1-Атаковать 2-Бежать 3-Выход > 3
Ты покидаешь подземелье. Прощай, Артас!

Спасибо за игру!
```

Не показываю код — только результат. Мы к нему придём шаг за шагом.

### Карта статьи

|#|Тема|Что освоим|
|---|---|---|
|1|Каркас|Точка входа — `main`|
|2–3|Ввод/вывод|`Scanner`, `System.out`, `printf`|
|4|Ветвление|`switch` expression, `if` vs `switch`|
|5–7|Циклы|`while` (игровой цикл), `for`, `break`/`continue`|
|8|Финал|Полный рабочий Dark Dungeon|

---

## 1. Каркас Dark Dungeon

Java

```
public class DarkDungeon {
    public static void main(String[] args) {
        // Здесь начнётся приключение
    }
}
```

Если вы читали предыдущую статью — `public`, `static`, `void`, `main` и `String[] args` уже знакомы. Если нет, вот суть в одном абзаце: **`main`** — точка входа, которую JVM вызывает при запуске. **`public static`** — чтобы JVM могла вызвать метод без создания объекта. **`void`** — метод ничего не возвращает. **`String[] args`** — аргументы командной строки (пока не используем).

Двигаемся дальше.

---

## ЧАСТЬ 1: ДИАЛОГ С ИГРОКОМ

## 2. `System.out` — вывод в консоль

Мы уже знаем, как программа «говорит»:

Java

```
System.out.println("=== DARK DUNGEON ===");
System.out.println("Добро пожаловать в подземелье!");
System.out.println();  // пустая строка
```

Два метода, одно отличие:

|Метод|Что делает|Когда использовать|
|---|---|---|
|`println()`|Печатает + переводит строку|Обычный вывод|
|`print()`|Печатает **без** перевода строки|Приглашение ввода (`>` )|

Java

```
System.out.print("Как тебя зовут, странник? > ");
// Курсор остаётся на той же строке — ждёт ввода
```

Говорить умеем. Пора научиться слушать.

---

## 3. `Scanner` — ввод от игрока

### 3.1. Подключаем Scanner

`System.out` — рот программы. `Scanner` — её уши: он читает то, что пользователь набирает на клавиатуре.

Java

```
import java.util.Scanner;

public class DarkDungeon {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("Как тебя зовут, странник? > ");
        String heroName = scanner.nextLine();

        System.out.println("Добро пожаловать, " + heroName + "!");
    }
}
```

Ключевые моменты:

- **`System.in`** — поток ввода с клавиатуры (симметрично `System.out` — потоку вывода).
- **`new Scanner(...)`** — создаём объект-сканер. Слово `new` и концепция объектов — тема статей про ООП. Пока запомните: «создаём сканер, который слушает клавиатуру».
- **`scanner.nextLine()`** — читает строку целиком, до нажатия Enter.

> ⚠️ **Не закрывайте `Scanner(System.in)`!** В примерах вы могли встречать `scanner.close()`. В наших программах этого делать не нужно. Причина техническая: `close()` закрывает не только `Scanner`, но и переданный ему `System.in`. А `System.in` — глобальный поток ввода, который **нельзя переоткрыть** в рамках JVM-процесса. После `close()` любая попытка чтения из `System.in` — даже через другой `Scanner` — упадёт с ошибкой.

### 3.2. Читаем числа: ловушка `nextInt()`

Для выбора класса героя нужно прочитать число. Наивный подход — использовать `nextInt()`:

**❌ Демонстрация бага** (не добавляйте в проект — это пример «как не надо»):

Java

```
Scanner scanner = new Scanner(System.in);

System.out.print("Введи уровень героя: > ");
int level = scanner.nextInt();       // Читаем число

System.out.print("Как тебя зовут? > ");
String heroName = scanner.nextLine(); // Читаем строку

System.out.println("Герой: " + heroName + ", уровень: " + level);
```

Если ввести `5` и нажать Enter:

text

```
Введи уровень героя: > 5
Как тебя зовут? >
Герой: , уровень: 5
```

Имя — пустое. Программа не дала его ввести.

**Что произошло?** Когда вы набираете `5` и нажимаете Enter, в буфер ввода попадают два символа: `5` и `\n` (перевод строки). `nextInt()` забирает `5`, но **оставляет** `\n` в буфере. Следующий `nextLine()` видит этот `\n` и решает, что пользователь уже ответил — пустой строкой.

**✅ Правильный подход — всегда `nextLine()` + парсинг:**

Java

```
Scanner scanner = new Scanner(System.in);

System.out.print("Введи уровень героя: > ");
int level = Integer.parseInt(scanner.nextLine().trim());

System.out.print("Как тебя зовут? > ");
String heroName = scanner.nextLine().trim();

System.out.println("Герой: " + heroName + ", уровень: " + level);
```

> 📌 **Правило: один `Scanner` — один метод чтения: `nextLine()`.** Числа — через `Integer.parseInt()`. Дробные — через `Double.parseDouble()`. **Не смешивайте** `nextInt()`/`nextDouble()` с `nextLine()` в одной программе.

**Зачем `.trim()`?** Убирает случайные пробелы по краям строки. Пользователь ввёл `" 42 "` — `.trim()` превратит в `"42"`, и `parseInt()` корректно его обработает.

> 💡 У `Scanner` есть метод `hasNextInt()`, который проверяет, является ли следующий токен числом. Он полезен при чтении через `nextInt()`, но при нашем паттерне `nextLine()` + `parseInt()` не понадобится.

### 3.3. Промежуточный результат

Java

```
import java.util.Scanner;

public class DarkDungeon {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.println("=== DARK DUNGEON ===");
        System.out.println();

        System.out.print("Как тебя зовут, странник? > ");
        String heroName = scanner.nextLine().trim();

        System.out.print("Выбери класс (1-Маг, 2-Воин, 3-Лучник): > ");
        int classChoice;
        try {
            classChoice = Integer.parseInt(scanner.nextLine().trim());
        } catch (NumberFormatException e) {
            System.out.println("Это не число. Назначаем класс по умолчанию.");
            classChoice = -1; // не соответствует ни одному case
        }

        int hp = 100; // hit points

        System.out.println();
        System.out.println("Выбор: " + classChoice + " | HP: " + hp);
        System.out.println("Добро пожаловать, " + heroName + "!");
    }
}
```

Пока `classChoice` — просто число. В следующем разделе мы превратим его в название класса через `switch`.

Обратите внимание: ввод числа обёрнут в `try-catch`. Если пользователь напечатает `"абракадабра"` или просто нажмёт Enter, `Integer.parseInt()` бросит `NumberFormatException`. Без `try-catch` программа упадёт. С ним — корректно обработает ошибку. Подробнее об исключениях — в будущих статьях; пока используйте эту конструкцию как защитный шаблон.

**Почему `-1`, а не `0`?** Это _sentinel-значение_ — число, которое заведомо не совпадает ни с одним допустимым `case` (1, 2, 3). Если кто-то позже добавит `case 0`, программа не сломается молча.

---

## 4. Выбор класса: `switch` expression

### 4.1. Старый `switch` — и почему он опасен

У нас есть конкретные пункты: 1, 2, 3. Для такой задачи существует `switch`. Но в Java живут две формы этой конструкции. Начнём с той, которую **не стоит** использовать:

Java

```
// ❌ Старый switch-statement
String heroClass;
switch (choice) {
    case 1:
        heroClass = "Маг";
        break;
    case 2:
        heroClass = "Воин";
        // Упс, забыли break!
    case 3:
        heroClass = "Лучник";
        break;
    default:
        heroClass = "Бродяга";
        break;
}
```

Видите баг? Если игрок выберет `2`, переменная `heroClass` станет `"Лучник"`, а не `"Воин"`. Без `break` выполнение «проваливается» (fall-through) в следующий `case`.

IDE вроде IntelliJ IDEA предупреждают об этом, как и флаг компилятора `javac -Xlint:fallthrough`. Но предупреждение — не ошибка компиляции. Код скомпилируется и запустится с багом. А у `switch` expression такой проблемы нет конструктивно.

### 4.2. `switch` expression (Java 14+) — наш выбор

Java

```
System.out.print("Выбери класс (1-Маг, 2-Воин, 3-Лучник): > ");
int classChoice = Integer.parseInt(scanner.nextLine().trim());

String heroClass = switch (classChoice) {
    case 1 -> "Маг";
    case 2 -> "Воин";
    case 3 -> "Лучник";
    default -> "Бродяга";
};
```

Три ключевых отличия от старого `switch`:

1. **Нет fall-through.** Стрелка `->` выполняет только свою ветку. Забыть `break` невозможно — его нет в синтаксисе.
2. **Возвращает значение.** Результат `switch` записывается прямо в переменную.
3. **Компилятор следит за полнотой.** При работе с `enum` (рассмотрим позже) компилятор потребует обработать все варианты.

**`default`** — страховочная ветка. Если игрок введёт `7` или `999`, программа не сломается — просто назначит «Бродягу».

#### `yield`: когда в ветке несколько строк

Если внутри ветки нужна логика — используйте блок `{}` и ключевое слово `yield`:

Java

```
String heroClass = switch (classChoice) {
    case 1 -> "Маг";
    case 2 -> "Воин";
    case 3 -> "Лучник";
    default -> {
        System.out.println("Неизвестный класс. Назначаем Бродягу.");
        yield "Бродяга";
    }
};
```

`yield` — аналог `return`, но для `switch` expression. Говорит: «вот значение этой ветки, запиши его в переменную».

#### Множественные метки

Несколько значений с одним результатом — через запятую:

Java

```
String archetype = switch (classChoice) {
    case 1, 3 -> "Дальний бой";   // Маг и Лучник
    case 2    -> "Ближний бой";    // Воин
    default   -> "Неизвестно";
};
```

### 4.3. `System.out.printf` — форматированный вывод

До сих пор мы склеивали строки через `+`:

Java

```
System.out.println("Добро пожаловать, " + heroName + "! Класс: " + heroClass);
```

При большом количестве переменных это превращается в кашу. Есть способ лучше:

Java

```
System.out.printf("Привет, %s! Твой класс: %s%n", heroName, heroClass);
System.out.printf("HP: %d%n", hp);
```

|Спецификатор|Тип|Пример результата|
|---|---|---|
|`%s`|строка (`String`)|`Артас`|
|`%d`|целое число (`int`)|`100`|
|`%f`|дробное число (`double`)|`3.140000`|
|`%.2f`|дробное с 2 знаками после запятой|`3.14`|
|`%n`|перенос строки (кроссплатформенный)|—|

> 📌 `printf` **не добавляет** перенос строки автоматически. Не забывайте `%n` в конце.

### 4.4. `if` vs `switch` — когда что

|Ситуация|Инструмент|Почему|
|---|---|---|
|HP в диапазоне: `hp > 70`, `hp > 30`|`if / else if`|Работает с `>`, `<`, `>=`|
|Действие игрока: `1`, `2`, `3`|`switch`|Конкретные значения|
|Составное условие: `isGameRunning && hp > 0`|`if` / условие `while`|Комбинация нескольких проверок|

> 📌 **Мнемоника:** `switch` = **меню** (конкретные пункты). `if` = **проверка** (диапазоны, комбинации).

> ⚡ **Правило фигурных скобок:** всегда ставьте `{}` в `if`/`else`/`for`/`while`, даже если тело — одна строка. Без скобок легко добавить строку «мимо» ветки:
> 
> Java
> 
> ```
> // ❌ Опасно:
> if (hp <= 0)
>     System.out.println("Game Over");
>     isGameRunning = false; // Выполнится ВСЕГДА — она вне if!
> 
> // ✅ Безопасно:
> if (hp <= 0) {
>     System.out.println("Game Over");
>     isGameRunning = false;
> }
> ```

### 4.5. Промежуточный результат Части 1

Java

```
import java.util.Scanner;

public class DarkDungeon {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // === ИНИЦИАЛИЗАЦИЯ ===
        System.out.println("=== DARK DUNGEON ===");
        System.out.println();

        System.out.print("Как тебя зовут, странник? > ");
        String heroName = scanner.nextLine().trim();

        System.out.print("Выбери класс (1-Маг, 2-Воин, 3-Лучник): > ");
        int classChoice;
        try {
            classChoice = Integer.parseInt(scanner.nextLine().trim());
        } catch (NumberFormatException e) {
            classChoice = -1;
        }

        String heroClass = switch (classChoice) {
            case 1 -> "Маг";
            case 2 -> "Воин";
            case 3 -> "Лучник";
            default -> {
                System.out.println("Неизвестный класс. Ты будешь Бродягой.");
                yield "Бродяга";
            }
        };

        int hp = 100; // hit points

        System.out.println();
        System.out.printf("Привет, %s! Твой класс: %s | HP: %d%n",
                          heroName, heroClass, hp);
    }
}
```

Попробуйте ввести `1`, `2`, `3`, `7`, `abc`, пустую строку. Убедитесь, что программа не падает ни в одном случае.

Работает. Но — один раз. Выбрал класс, увидел приветствие — и программа закрылась. Герой даже не дошёл до монстра.

Нам нужен цикл.

---

## ЧАСТЬ 2: ИГРОВОЙ ЦИКЛ

## 5. Концепция: сердце любой игры

Каждая игра — от тетриса до Elden Ring — работает по одному принципу:

text

```
ПОКА (игра запущена) {
    1. Считай ввод игрока
    2. Обнови состояние мира
    3. Покажи результат
}
```

В Unity это `Update()`. В Unreal — `Tick()`. В нашем Dark Dungeon — `while`.

### Три вида циклов в Java

|Цикл|Когда использовать|Пример из RPG|
|---|---|---|
|`while`|Не знаем, сколько итераций|Бой: пока кто-то жив|
|`for`|Знаем, сколько итераций|Серия из 3 ударов|
|`do-while`|Как `while`, но минимум одна итерация гарантирована|Встречается реже; в этой статье не используем|

Начнём с `while` — он нужен прямо сейчас.

---

## 6. `while` — запускаем игровой цикл

### 6.1. Оборачиваем игру в цикл

Добавляем после инициализации:

Java

```
boolean isGameRunning = true;

while (isGameRunning && hp > 0) {
    System.out.println();
    System.out.println("------------------------------");
    System.out.printf("%s | HP: %d%n", heroName, hp);
    System.out.println("Перед тобой стоит монстр!");
    System.out.print("Что делаешь? 1-Атаковать 2-Бежать 3-Выход > ");

    int action;
    try {
        action = Integer.parseInt(scanner.nextLine().trim());
    } catch (NumberFormatException e) {
        System.out.println("Введи число, странник!");
        continue; // ← пропустить остаток итерации, начать заново
    }

    System.out.println("Ты выбрал: " + action);
}
```

Обратите внимание на **два условия** в заголовке цикла: `isGameRunning` (игрок не вышел) **и** `hp > 0` (герой жив). Если любое из них станет `false` — цикл завершится. Проверка в заголовке `while` надёжнее, чем отдельный `if` в теле цикла: между `if` и концом тела можно случайно вставить код, и проверка перестанет работать вовремя.

`continue` мы разберём подробнее чуть ниже. Пока достаточно знать: он перезапускает текущую итерацию с самого начала.

> 💡 **Ctrl+C** в консоли — аварийная остановка. Пока мы не написали обработку выхода, это ваш стоп-кран.

### 6.2. Случайный монстр и `Math.random()`

Сделаем каждую встречу уникальной:

Java

```
String[] monsters = {"Гоблин", "Скелет", "Тролль", "Огр", "Дракон"};
```

> Массивы мы подробно разберём в следующей статье. Пока достаточно знать: это список значений, доступных по индексу (начиная с 0). `monsters[0]` → `"Гоблин"`, `monsters[4]` → `"Дракон"`.

Выбираем случайного монстра:

Java

```
int monsterIndex = (int) (Math.random() * monsters.length);
String monster = monsters[monsterIndex];
System.out.printf("Перед тобой стоит %s!%n", monster);
```

`Math.random()` возвращает случайное дробное число от `0.0` (включительно) до `1.0` (не включительно). Умножаем на количество элементов, отбрасываем дробную часть через `(int)` — получаем индекс от `0` до `length - 1`.

**Общая формула для случайного целого в диапазоне [min, max]:**

Java

```
int random = (int) (Math.random() * (max - min + 1)) + min;
```

Например, урон от 10 до 39:

Java

```
// rangeSize = 39 - 10 + 1 = 30
int damage = (int) (Math.random() * 30) + 10;
```

`Math.random() * 30` → дробное из `[0.0, 30.0)` → `(int)` отсекает дробную часть → `[0, 29]` → `+ 10` → `[10, 39]`.

### 6.3. Обработка выбора: атака, бегство, выход

Обрабатываем действие через `switch` expression, результат которого — `boolean`, управляющий циклом:

Java

```
isGameRunning = switch (action) {
    case 1 -> {
        // === АТАКА ===
        double luck = Math.random();
        if (luck > 0.4) {
            System.out.printf(
                "Ты наносишь мощный удар! %s повержен!%n", monster);
        } else {
            int damage = (int) (Math.random() * 30) + 10; // [10, 39]
            hp -= damage;
            System.out.printf(
                "%s бьёт в ответ! -%d HP!%n", monster, damage);
        }
        yield true;  // продолжаем
    }
    case 2 -> {
        // === БЕГСТВО ===
        System.out.println("Ты убегаешь обратно к развилке...");
        yield true;  // продолжаем
    }
    case 3 -> {
        // === ВЫХОД ===
        System.out.printf(
            "Ты покидаешь подземелье. Прощай, %s!%n", heroName);
        yield false; // останавливаем цикл
    }
    default -> {
        System.out.println("Нет такого действия. Попробуй ещё.");
        yield true;
    }
};
```

Разберём механику:

- **`Math.random() > 0.4`** — 60% шанс победы. Значения от 0.4 до 1.0 составляют 60% диапазона `[0.0, 1.0)`.
- **`if` / `else` внутри `switch`** — идеальное сочетание. `switch` выбирает _действие_ (что делать), `if` определяет _исход_ (удалось или нет).
- **`yield true / false`** — результат записывается в `isGameRunning`. Когда `false`, на следующей проверке `while (isGameRunning && hp > 0)` цикл завершится.

Если монстр нанёс урон и HP упало ниже нуля, `yield true` не помешает завершению: на следующей итерации `hp > 0` вернёт `false`, и цикл остановится.

### 6.4. `break` и `continue` — управление потоком

#### `continue` — «попробуй ещё раз»

Мы уже использовали `continue` при обработке ввода:

Java

```
try {
    action = Integer.parseInt(scanner.nextLine().trim());
} catch (NumberFormatException e) {
    System.out.println("Введи число, странник!");
    continue;  // пропустить остаток итерации, начать следующую
}
```

`continue` говорит: «Забудь эту итерацию, начни следующую». Программа возвращается к `while (...)`, проверяет условие и, если оно `true`, выводит нового монстра.

#### `break` — полный выход из цикла

`break` прекращает цикл целиком. В нашем коде мы используем **флаг `isGameRunning`** вместо `break`, и вот почему:

В `switch` expression (с `->`) ключевое слово `break` **запрещено** компилятором. Каждая ветка обязана вернуть значение через `yield` (или через одиночное выражение). `break` не возвращает значение, поэтому не может быть использован ни в одной ветке — ни как выход из `switch`, ни как выход из внешнего цикла.

Флаг `isGameRunning` решает задачу чисто: он читается как естественный язык — «пока игра запущена и герой жив — продолжаем».

|Ключевое слово|Действие|
|---|---|
|`break`|Выйти из цикла **совсем**|
|`continue`|Пропустить **текущую** итерацию, начать следующую|

### 6.5. Статус здоровья — `if` / `else if` в действии

Добавим индикатор состояния героя:

Java

```
String status = "Неизвестно";
if (hp > 70) {
    status = "Здоров";
} else if (hp > 30) {
    status = "Ранен";
} else {
    status = "При смерти!";
}
System.out.printf("%s | HP: %d (%s)%n", heroName, hp, status);
```

Здесь `switch` не подойдёт: мы проверяем **диапазоны** (`> 70`, `> 30`), а не конкретные значения.

Зачем инициализировать `status = "Неизвестно"`? Компилятор Java проверяет, что переменная инициализирована перед использованием. Здесь все ветки (`if` / `else if` / `else`) покрыты, и компилятор это понимает. Но явная инициализация — полезная привычка: если позже кто-то уберёт ветку `else`, код без инициализации перестанет компилироваться, а с ней — выдаст осмысленное значение по умолчанию.

---

## 7. `for` — когда количество известно

`while` крутится, пока условие истинно. Но иногда мы точно знаем: нужно повторить **N раз**. Для этого есть `for`.

Допустим, мы хотим вывести список из трёх предметов:

Java

```
String[] loot = {"Зелье HP", "Ржавый меч", "Золотая монета"};
System.out.println("В сундуке:");

for (int i = 0; i < loot.length; i++) {
    System.out.printf("  %d. %s%n", i + 1, loot[i]);
}
```

Вывод:

text

```
В сундуке:
  1. Зелье HP
  2. Ржавый меч
  3. Золотая монета
```

**Анатомия `for`:**

Java

```
for (int i = 0; i < 3; i++) {
//   ^^^^^^^^^   ^^^^^^  ^^^
//   старт       условие  шаг
}
```

|Часть|Что делает|
|---|---|
|**Старт:** `int i = 0`|Создаём счётчик, начинаем с 0|
|**Условие:** `i < 3`|Пока `true` — продолжаем|
|**Шаг:** `i++`|После каждой итерации увеличиваем счётчик|

> Этот пример — демонстрация синтаксиса `for`. В финальный код Dark Dungeon мы его не добавляем, чтобы не усложнять, но вы можете добавить его самостоятельно (например, выпадение лута после победы).

### `while` vs `for` — сравнение

|Критерий|`while`|`for`|
|---|---|---|
|Знаем сколько раз?|Нет|Да|
|Типичный пример|Игровой цикл, ожидание|Перебор массива, фиксированные повторы|
|Условие завершения|Зависит от состояния|Зависит от счётчика|

> 📌 **Мнемоника:** знаешь сколько раз → `for`. Не знаешь → `while`.

---

## 8. Финал: полный код Dark Dungeon

### 8.1. `DarkDungeon.java`

Java

```
import java.util.Scanner;

public class DarkDungeon {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // === ИНИЦИАЛИЗАЦИЯ ===
        System.out.println("=== DARK DUNGEON ===");
        System.out.println();

        System.out.print("Как тебя зовут, странник? > ");
        String heroName = scanner.nextLine().trim();

        System.out.print("Выбери класс (1-Маг, 2-Воин, 3-Лучник): > ");
        int classChoice;
        try {
            classChoice = Integer.parseInt(scanner.nextLine().trim());
        } catch (NumberFormatException e) {
            classChoice = -1; // sentinel — не совпадает ни с одним case
        }

        String heroClass = switch (classChoice) {
            case 1 -> "Маг";
            case 2 -> "Воин";
            case 3 -> "Лучник";
            default -> {
                System.out.println("Неизвестный класс. Ты будешь Бродягой.");
                yield "Бродяга";
            }
        };

        int hp = 100; // hit points

        System.out.println();
        System.out.printf("Привет, %s! Твой класс: %s | HP: %d%n",
                          heroName, heroClass, hp);

        // === МОНСТРЫ ===
        String[] monsters = {"Гоблин", "Скелет", "Тролль", "Огр", "Дракон"};

        // === ИГРОВОЙ ЦИКЛ ===
        boolean isGameRunning = true;

        while (isGameRunning && hp > 0) {
            System.out.println();
            System.out.println("------------------------------");

            // Статус здоровья
            String status = "Неизвестно";
            if (hp > 70) {
                status = "Здоров";
            } else if (hp > 30) {
                status = "Ранен";
            } else {
                status = "При смерти!";
            }
            System.out.printf("%s | HP: %d (%s)%n", heroName, hp, status);

            // Случайный монстр
            int monsterIndex = (int) (Math.random() * monsters.length);
            String monster = monsters[monsterIndex];
            System.out.printf("Перед тобой стоит %s!%n", monster);

            // Ввод действия
            System.out.print("Что делаешь? 1-Атаковать 2-Бежать 3-Выход > ");

            int action;
            try {
                action = Integer.parseInt(scanner.nextLine().trim());
            } catch (NumberFormatException e) {
                System.out.println("Введи число, странник!");
                continue;
            }

            // Обработка выбора
            isGameRunning = switch (action) {
                case 1 -> {
                    // Атака
                    double luck = Math.random();
                    if (luck > 0.4) {
                        System.out.printf(
                            "Ты наносишь мощный удар! %s повержен!%n",
                            monster);
                    } else {
                        int damage = (int) (Math.random() * 30) + 10;
                        hp -= damage;
                        System.out.printf(
                            "%s бьёт в ответ! -%d HP!%n",
                            monster, damage);
                    }
                    yield true;
                }
                case 2 -> {
                    // Бегство
                    System.out.println(
                        "Ты убегаешь обратно к развилке...");
                    yield true;
                }
                case 3 -> {
                    // Выход
                    System.out.printf(
                        "Ты покидаешь подземелье. Прощай, %s!%n",
                        heroName);
                    yield false;
                }
                default -> {
                    System.out.println(
                        "Нет такого действия. Попробуй ещё.");
                    yield true;
                }
            };
        }

        // === КОНЕЦ ИГРЫ ===
        if (hp <= 0) {
            System.out.println();
            System.out.printf("*** %s пал в подземелье... Game Over. ***%n",
                              heroName);
        }

        System.out.println();
        System.out.println("Спасибо за игру!");
    }
}
```

> **Примечание для Windows.** Если в стандартном `cmd.exe` символы отображаются некорректно — запускайте через Windows Terminal или консоль IntelliJ IDEA. Они корректно работают с UTF-8.

### 8.2. Тестовый прогон

text

```
=== DARK DUNGEON ===

Как тебя зовут, странник? > Артас
Выбери класс (1-Маг, 2-Воин, 3-Лучник): > 2
Привет, Артас! Твой класс: Воин | HP: 100

------------------------------
Артас | HP: 100 (Здоров)
Перед тобой стоит Скелет!
Что делаешь? 1-Атаковать 2-Бежать 3-Выход > 1
Ты наносишь мощный удар! Скелет повержен!

------------------------------
Артас | HP: 100 (Здоров)
Перед тобой стоит Дракон!
Что делаешь? 1-Атаковать 2-Бежать 3-Выход > 1
Дракон бьёт в ответ! -27 HP!

------------------------------
Артас | HP: 73 (Здоров)
Перед тобой стоит Тролль!
Что делаешь? 1-Атаковать 2-Бежать 3-Выход > 2
Ты убегаешь обратно к развилке...

------------------------------
Артас | HP: 73 (Здоров)
Перед тобой стоит Гоблин!
Что делаешь? 1-Атаковать 2-Бежать 3-Выход > абракадабра
Введи число, странник!

------------------------------
Артас | HP: 73 (Здоров)
Перед тобой стоит Огр!
Что делаешь? 1-Атаковать 2-Бежать 3-Выход > 3
Ты покидаешь подземелье. Прощай, Артас!

Спасибо за игру!
```

Запустите. Попробуйте ввести буквы вместо цифр. Атакуйте, пока не убьют. Выйдите сразу. Нажмите Enter без текста. Проверьте, что программа корректно обрабатывает всё.

---

## 9. Чек-лист: что мы изучили

|Концепция|Ключевое правило|
|---|---|
|`Scanner`|Один метод чтения: `nextLine()`. Целые числа — `Integer.parseInt()`, дробные — `Double.parseDouble()`. Не смешивать `nextInt()` с `nextLine()`.|
|`System.out`|`println` — с переводом строки. `print` — без. `printf` — форматированный вывод: `%s`, `%d`, `%.2f`, `%n`.|
|`switch` expression (`->`)|Для меню и конкретных значений. Нет fall-through. Возвращает значение. Многострочная ветка — блок `{}` + `yield`.|
|`if` / `else if`|Для диапазонов, комбинаций, сложных условий. Всегда с `{}`.|
|`while`|Не знаем сколько итераций → `while`. Игровой цикл = `while`.|
|`for`|Знаем сколько итераций → `for`.|
|`break` / `continue`|`break` — выход из цикла. `continue` — пропуск текущей итерации.|
|`Math.random()`|Число от `0.0` до `1.0`. Диапазон `[min, max]`: `(int)(Math.random() * (max - min + 1)) + min`.|
|Фигурные скобки|Всегда ставьте `{}` в `if`/`else`/`for`/`while`, даже для одной строки.|