## Как работает интернет: IP, TCP, HTTP — для будущих Java-разработчиков

---

## 1. Введение: Интернет — это просто провода и текст

Вы нажимаете **Enter** в строке браузера. Через 200 миллисекунд на экране — картинка, текст, кнопки. Что произошло за эти 200 мс?

**Ничего волшебного.**

Один компьютер отправил другому компьютеру текстовое сообщение по проводу (или радиоволне). Второй компьютер ответил текстом. Браузер нарисовал этот текст красиво.

Всё.

**Формула, которую стоит запомнить прямо сейчас:**

text

```
IP  →  TCP  →  HTTP  →  Текст  →  Ответ
```

**Зачем это Java-разработчику?** Ваш будущий backend-сервер — это программа, которая принимает текстовый запрос, обрабатывает его и возвращает текст (JSON или HTML). Понимание этой цепочки — фундамент, без которого Spring Boot, сервлеты и REST API будут казаться магией.

Давайте разберём каждое звено.

---

## 2. Адресация: Как найти нужный компьютер в сети

Первый вопрос: **КУДА** летит запрос?

Чтобы отправить письмо, нужен адрес. В интернете адрес состоит из трёх частей: **имя**, **IP** и **порт**. Разберём каждую.

---

### 2.1. IP-адрес — адрес дома

IP-адрес — это **уникальный числовой адрес устройства в сети**. Каждый компьютер, подключённый к интернету, имеет IP. Без него данные просто не найдут, куда лететь.

Существует два формата:

text

```
IPv4:  93.184.216.34
       (четыре числа от 0 до 255, разделённые точками)

IPv6:  2606:2800:220:1:248:1893:25c8:1946
       (длиннее, но та же идея — уникальный адрес)
```

IPv4-адресов всего около 4,3 миллиарда. Когда интернет был маленьким, этого хватало. Сейчас — нет. IPv6 решает проблему нехватки, но IPv4 по-прежнему встречается повсеместно.

**Аналогия:**

> IP — это почтовый адрес многоквартирного дома.  
> Без адреса почтальон не доставит письмо, а пакет данных не найдёт сервер.

**Где аналогия ломается** (и это нормально):

- Один IP может обслуживать тысячи сайтов (virtual hosting). Как если бы по одному адресу стояли тысячи зданий.
- IP бывает **динамическим** — провайдер выдаёт вам новый адрес при каждом подключении. «Дом переезжает».
- За одним IP может прятаться целый дата-центр с балансировщиком нагрузки.

Для старта достаточно: **IP = числовой адрес машины в сети.**

---

### 2.2. Порт — номер квартиры

Итак, IP привёл нас к нужному компьютеру. Но на одном компьютере может работать десяток программ одновременно: веб-сервер, база данных, почтовый сервер, SSH... Как данные узнают, какой именно программе они предназначены?

Для этого существует **порт**.

Порт — это число от `0` до `65535`. Логический идентификатор конкретного процесса (программы) на машине.

**Аналогия:**

> Если IP — это адрес многоквартирного дома, то порт — **номер квартиры**.
> 
> Веб-сервер живёт в квартире **80** (или **443**).  
> База данных PostgreSQL — в квартире **5432**.  
> Ваш Spring Boot — в квартире **8080**.
> 
> Все они в одном «доме» (на одном IP), но каждый слушает свой «дверной звонок» (порт).

**Стандартные порты-ориентиры:**

| Порт   | Сервис               | Когда встретите          |
| ------ | -------------------- | ------------------------ |
| `80`   | HTTP                 | Обычные сайты            |
| `443`  | HTTPS                | Сайты с замочком 🔒      |
| `22`   | SSH                  | Подключение к серверу    |
| `5432` | PostgreSQL           | Работа с базой данных    |
| `3306` | MySQL                | Ещё одна популярная БД   |
| `8080` | Tomcat / Spring Boot | Каждый день в разработке |

**Где аналогия ломается:** порт — не физическая комната. Это логическая метка. Программа «слушает» (`listen`) определённый порт. Если никто не слушает — данные уйдут в пустоту, и вы получите ошибку `Connection refused`.

---

> ### ⚠️ Ситуация, которая случится с каждым из вас
> 
> Вы запускаете Spring Boot и видите:
> 
> text
> 
> ```
> Web server failed to start. Port 8080 was already in use.
> ```
> 
> **Что произошло:** в «квартиру 8080» уже заселилась другая программа. Два процесса не могут слушать один порт одновременно. Вы, возможно, забыли остановить предыдущий запуск — и он всё ещё занимает порт.
> 
> **Что делать:**
> 
> 1. Найти и остановить программу, занявшую порт
> 2. Или запустить свой сервер на другом порту (`8081`, `9090`...)
> 
> Это **самый частый** вопрос начинающих на Stack Overflow. Теперь вы знаете ответ.

---

### 2.3. DNS — телефонная книга интернета

Проблема очевидна: **мы не помним числа**.

Вы помните `google.com`. Вы помните `youtube.com`. Но вы не помните `142.250.74.46`. И не должны.

Для этого существует **DNS** — Domain Name System. Это распределённая система серверов по всему миру, которая переводит **человекопонятное имя** в **IP-адрес**.

**Аналогия:**

> DNS — это контакты в вашем телефоне.  
> Вы нажимаете «Мама», а телефон набирает `+7-999-123-45-67`.  
> Вы вводите `google.com`, а DNS возвращает `142.250.74.46`.

**Что происходит, когда вы вводите `google.com` в браузере:**

text

```
┌──────────┐  «Какой IP у google.com?»   ┌──────────────┐
│          │ ───────────────────────────► │              │
│ Браузер  │                              │  DNS-сервер  │
│          │ ◄─────────────────────────── │              │
└──────────┘  «142.250.74.46»            └──────────────┘
      │
      │  Теперь знаю куда идти!
      ▼
┌──────────────────┐
│  Сервер Google   │
│  142.250.74.46   │
└──────────────────┘
```

**По шагам:**

1. Браузер спрашивает у ОС: «Какой IP у `google.com`?»
2. ОС проверяет локальный кэш (может, уже спрашивали недавно)
3. Если не знает — спрашивает DNS-сервер провайдера
4. Тот, если не знает, идёт выше по иерархии DNS-серверов
5. Ответ найден: `142.250.74.46`
6. Браузер стучится на этот IP

**Проверьте сами прямо сейчас.** Откройте терминал:

Bash

```
nslookup google.com
```

Результат (примерно):

text

```
Server:     192.168.1.1          # ← Ваш DNS-сервер (у вас будет свой — это нормально)
Address:    192.168.1.1#53

Non-authoritative answer:
Name:   google.com
Address: 142.250.74.46           # ← Вот он, IP-адрес!
```

> 💡 **Примечание:** у вас строка `Server` будет отличаться — это IP вашего роутера или провайдера. Это нормально. Важна строка `Address` в секции ответа — это и есть IP, в который DNS перевёл имя.

Или, если у вас Linux/macOS, попробуйте `dig` — он даёт больше деталей:

Bash

```
dig google.com
```

text

```
;; ANSWER SECTION:
google.com.     197     IN      A       142.250.74.46
```

Строка `A 142.250.74.46` — это ответ DNS: имя `google.com` соответствует IP-адресу `142.250.74.46`.

**Где аналогия с телефонной книгой ломается:**

- DNS — не одна книга, а **иерархия серверов** по всему миру
- Ответы **кэшируются** с TTL (Time To Live). Через какое-то время кэш протухает, и нужно спрашивать заново
- Возможны атаки: **DNS spoofing** — подмена «номера телефона». Вы набираете «Мама», а вас соединяют с мошенником

---

### 2.4. localhost:8080 — ваш ежедневный адрес

Это, пожалуй, **самый важный адрес**, который вы будете набирать как Java-разработчик. Каждый день. Много раз в день.

**Что такое `localhost`?**

> `localhost` — специальное имя, которое **ВСЕГДА** указывает на **ваш собственный компьютер**.
> 
> IP-эквивалент: `127.0.0.1` (IPv4) или `::1` (IPv6)

Когда вы обращаетесь к `127.0.0.1`, запрос **НЕ выходит в интернет**. Вообще никуда не уходит. Он замыкается внутри операционной системы через так называемый **loopback-интерфейс**. Это петля: компьютер отправляет сообщение самому себе.

text

```
┌─────────────────────────────────────────┐
│           Ваш компьютер                 │
│                                         │
│  ┌──────────┐     ┌──────────────────┐  │
│  │ Браузер  │────►│ Spring Boot      │  │
│  │          │◄────│ (порт 8080)      │  │
│  └──────────┘     └──────────────────┘  │
│        localhost:8080                    │
│        (127.0.0.1:8080)                 │
│                                         │
│  Запрос НЕ покидает эту коробку!        │
└─────────────────────────────────────────┘
```

**Аналогия:** разговаривать с самим собой перед зеркалом. Письмо не покидает квартиру.

**Что такое `localhost:8080`?**

> «Обратись к своему же компьютеру (`localhost`),  
> в квартиру `8080` (порт).  
> Там живёт мой Spring Boot сервер.»

**Почему сервер при разработке запускается именно на `localhost`?**

Когда вы пишете Java-приложение и запускаете его, оно начинает «слушать» определённый порт на вашей машине. По умолчанию Spring Boot слушает порт `8080`. Приложение говорит операционной системе: «Если кто-то постучится в порт 8080 — передавай мне».

Вы открываете браузер, вводите `http://localhost:8080`, и запрос уходит не в интернет, а к вашей же программе. Вы — и клиент, и сервер одновременно. Это идеальная песочница для разработки и отладки.

---

### Промежуточный итог по адресации

Чтобы «достучаться» до сервера, нужно знать **три вещи**:

| #   | Что      | Пример          | Роль                   |
| --- | -------- | --------------- | ---------------------- |
| 1   | **Имя**  | `google.com`    | DNS переведёт в IP     |
| 2   | **IP**   | `142.250.74.46` | Адрес машины           |
| 3   | **Порт** | `443`           | Какой сервис на машине |

Полный адрес:

text

```
google.com:443  →  142.250.74.46:443
    имя:порт           IP:порт
```

---

## 3. Разговор: Как два компьютера общаются

Адрес знаем. Теперь вопрос: **ЧТО именно летит по проводу?**

---

### 3.1. TCP — надёжная доставка (краткая подложка)

Прежде чем говорить про HTTP, нужно упомянуть TCP. HTTP работает **поверх** TCP. Без понимания «нижнего этажа» верхний висит в воздухе.

**Суть TCP в трёх предложениях:**

1. TCP **устанавливает соединение** между двумя машинами (так называемое «рукопожатие»)
2. **Гарантирует**, что все данные дойдут и в правильном порядке
3. Если пакет потерялся — TCP **переотправит**

**Аналогия:** заказное письмо с подтверждением вручения. Вы точно знаете, что дошло.

> 💡 **Важно для Java-разработчика:** Вы никогда не будете писать TCP вручную. Но когда увидите ошибку `Connection refused` или `Connection timed out` — будете знать, что сломалось именно на этом этапе. Сервер либо не слушает порт, либо не отвечает на «рукопожатие».

---

### 3.2. HTTP — текстовый протокол общения

Вот мы добрались до главного.

> ### 🔑 ГЛАВНЫЙ ИНСАЙТ ЭТОЙ СТАТЬИ
> 
> **HTTP — это обычный текст.**
> 
> Не бинарный формат. Не магия. Не зашифрованный поток.  
> Буквально строки символов, которые можно прочитать глазами.

**Модель общения — Request / Response (Запрос / Ответ):**

1. Клиент отправляет **запрос** (Request)
2. Сервер возвращает **ответ** (Response)

Одна пара: **один запрос → один ответ**. Всё.

**Аналогия — диалог у прилавка:**

text

```
Покупатель: «Мне кофе, пожалуйста»  ← Request
Продавец:   «Вот ваш кофе. 200 ₽»   ← Response
```

Покупатель не получит кофе, пока не попросит. Продавец не заговорит первым. Один вопрос — один ответ.

> 📝 **Примечание о версиях протокола.** В этой статье мы разбираем HTTP/1.1 — базовую версию, на которой построено всё понимание. В реальности вы будете встречать и HTTP/2 (например, `HTTP/2 200` в выводе curl). Не пугайтесь: HTTP/2 быстрее, но **принцип тот же** — запрос → ответ, те же методы, те же коды, те же заголовки. Всё, что вы сейчас выучите, работает и для HTTP/2.

---

### 3.3. Анатомия HTTP-сообщения

Любое HTTP-сообщение (и запрос, и ответ) состоит из **трёх частей**:

text

```
┌─────────────────────────────────────┐
│  1. Стартовая строка                │  ← ЧТО хочу / ЧТО случилось
├─────────────────────────────────────┤
│  2. Заголовки (Headers)             │  ← Метаданные
│     Key: Value                      │     (надписи на конверте)
│     Key: Value                      │
│     Key: Value                      │
├─────────────────────────────────────┤  ← Пустая строка-разделитель
│  3. Тело (Body)                     │  ← Содержимое
│     (опционально)                   │     (письмо внутри конверта)
└─────────────────────────────────────┘
```

---

#### 3.3.1. Стартовая строка

**В ЗАПРОСЕ** (Request Line):

text

```
GET /index.html HTTP/1.1
 │       │          │
 │       │          └─ Версия протокола
 │       └─ Путь (ЧТО хочу получить)
 └─ Метод (ЧТО хочу СДЕЛАТЬ)
```

_Человеческим языком:_ «Дай мне страницу `/index.html`, говорю по протоколу HTTP версии 1.1»

**В ОТВЕТЕ** (Status Line):

text

```
HTTP/1.1 200 OK
    │     │   │
    │     │   └─ Пояснение для человека
    │     └─ Код статуса (для программы)
    └─ Версия протокола
```

_Человеческим языком:_ «Говорю по HTTP/1.1, всё хорошо (200), вот держи ответ»

---

#### 3.3.2. Заголовки (Headers)

Формат: `Ключ: Значение`, по одному на строку.

**Аналогия:** надписи на конверте — кому, от кого, тип содержимого.

Заголовки, которые вы будете видеть **постоянно**:

| Заголовок       | Пример значения    | Что значит                                         |
| --------------- | ------------------ | -------------------------------------------------- |
| `Host`          | `example.com`      | Какой сайт запрашиваем (важно при virtual hosting) |
| `Content-Type`  | `application/json` | Формат тела сообщения: «внутри — JSON»             |
| `User-Agent`    | `curl/8.0`         | Кто спрашивает (браузер? curl? бот?)               |
| `Authorization` | `Bearer <token>`   | «Я имею право» — токен авторизации                 |

**Где аналогия с конвертом ломается:** заголовки не просто подписи. Они **управляют поведением** сервера: кэширование, авторизация, тип ответа, сжатие и многое другое.

---

#### 3.3.3. Тело (Body)

Что это: **основное содержимое** — то, ради чего всё затевалось. Может быть: HTML, JSON, XML, файл, текст, картинка.

**Аналогия:** письмо внутри конверта. Заголовки — надписи снаружи. Body — содержимое внутри.

**Правило:**

| Тип сообщения       | Тело?               | Почему            |
| ------------------- | ------------------- | ----------------- |
| `GET`-запрос        | Обычно **нет**      | Только спрашиваем |
| `POST`/`PUT`-запрос | Обычно **да**       | Отправляем данные |
| Ответ сервера       | Почти всегда **да** | Отдаём данные     |

---

### 3.4. HTTP-глаголы (методы) — что хочу сделать

HTTP-метод — это **глагол**. Он говорит серверу, ЧТО вы хотите сделать с ресурсом.

| Метод    | Человеческий смысл | Пример                                 | Тело? |
| -------- | ------------------ | -------------------------------------- | ----- |
| `GET`    | «Дай»              | Получить страницу, данные пользователя | Нет   |
| `POST`   | «На, создай»       | Создать заказ, отправить форму         | Да    |
| `PUT`    | «Замени целиком»   | Обновить профиль                       | Да    |
| `DELETE` | «Удали»            | Удалить запись                         | Нет   |

**GET — безопасный.** Как чтение книги в библиотеке. Вы взяли книгу, прочитали, поставили на место. Ничего не изменилось. Можно читать сколько угодно раз — результат один и тот же.

**POST — опасный.** Как отправка заказа в интернет-магазине. Вы нажали «Оплатить» — и состояние изменилось: деньги списались, заказ создался. Нажмёте второй раз — может создаться второй заказ. POST **меняет состояние** на сервере.

**Пример GET-запроса:**

http

```
GET /users/42 HTTP/1.1
Host: api.example.com

(тела нет)
```

_«Дай мне пользователя с ID 42»_

**Пример POST-запроса:**

http

```
POST /users HTTP/1.1
Host: api.example.com
Content-Type: application/json

{"name": "Alice", "email": "alice@mail.com"}
```

_«Создай нового пользователя, вот его данные»_

### 3.5. Коды ответа — светофор HTTP

Сервер всегда отвечает числовым кодом. Этот код — **первое**, на что смотрит программа (и разработчик при отладке).

Представьте светофор:

|Диапазон|Цвет|Что значит|
|---|---|---|
|**2xx**|🟢 Зелёный|**Всё хорошо.** Проезжай.|
|**3xx**|🟡 Жёлтый|**Иди в другое место** (перенаправление).|
|**4xx**|🔴 Красный|**Твоя вина, клиент.** Кривой запрос.|
|**5xx**|🔥 Пожар|**Вина сервера.** Сервер упал / сломался.|

**Конкретные коды, которые вы встретите первыми:**

|Код|Текст|Что значит|Когда встретите|
|---|---|---|---|
|`200`|OK|Всё хорошо, вот ответ|Каждый успешный запрос|
|`201`|Created|Ресурс создан|`POST` вернул новый объект|
|`301`|Moved Permanently|Переехал навсегда|Редирект сайта|
|`302`|Found|Переехал временно|Редирект после логина|
|`400`|Bad Request|Кривой запрос|Невалидный JSON|
|`401`|Unauthorized|Не авторизован|Забыли токен|
|`403`|Forbidden|Нет прав|Токен есть, доступа нет|
|`404`|Not Found|Ресурс не найден|Неверный URL|
|`500`|Internal Server Error|Сервер сломался|Баг в вашем Java-коде|

**Обратите внимание на разницу между `401` и `403`:**

text

```
401 — «Кто ты вообще? Представься!»     (нет токена / пароля)
403 — «Я тебя знаю, но тебе сюда нельзя» (токен есть, прав нет)
```

**И на разницу между `4xx` и `5xx`:**

text

```
4xx — клиент виноват. Сервер работает правильно, но запрос кривой.
5xx — сервер виноват. Запрос нормальный, но на сервере что-то упало.
```

> 💡 Когда вы будете писать Java-backend и увидите в логах `500` — это **ваш** баг. Когда фронтенд-разработчик скажет «мне прилетает `400`» — скорее всего, он шлёт кривые данные (но проверьте оба варианта).

---

### 3.6. Stateless — сервер вас не помнит

Критически важный факт о HTTP:

> **Каждый запрос независим. Сервер не знает, что предыдущий запрос тоже был от вас.**

**Аналогия:** продавец с амнезией. Вы подходите к прилавку, покупаете кофе. Отходите. Подходите снова — продавец смотрит на вас как на незнакомца. Каждый раз вы для него **новый покупатель**.

Чтобы он вас «узнал», нужно показать чек от предыдущей покупки. В HTTP роль такого чека играют:

- **Cookie** — маленький файл, который браузер автоматически отправляет с каждым запросом
- **Токен (JWT)** — строка в заголовке `Authorization`
- **Идентификатор сессии** — хранится на сервере, ключ передаётся в cookie

**Пример из жизни, который вы видите каждый день:**

Вы логинитесь на сайте, потом обновляете страницу — и вас **не** «выбрасывает». Почему? Потому что при логине сервер выдал вам cookie (или токен). Теперь браузер **тихо подкладывает** эту cookie в каждый следующий запрос. Сервер видит её и «вспоминает» вас.

Без cookie каждое обновление страницы отправляло бы вас обратно на экран логина.

**Почему это важно:** именно поэтому существуют сессии, куки, JWT-токены. Все механизмы «запоминания» — это надстройки поверх stateless-протокола. Понимание этого факта избавит вас от десятков багов в будущем.

---

### 3.7. HTTPS — тот же HTTP, но в конверте с сургучной печатью

**HTTPS = HTTP + TLS-шифрование.**

Данные шифруются. Третья сторона (провайдер, хакер в кафе с Wi-Fi, правительственный фильтр) видит, что вы общаетесь с сервером, но **не может прочитать** содержимое.

**Аналогия:** то же письмо, но в запечатанном сейф-конверте. Прочитать может только адресат.

text

```
HTTP:   «Привет, мой пароль: qwerty123»
         (любой по дороге может прочитать)

HTTPS:  «a7$kL9!xR2...» → расшифровывается на сервере
         → «Привет, мой пароль: qwerty123»
```

**Для разработчика:**

|Среда|Протокол|Пример|
|---|---|---|
|Локальная разработка|Обычно HTTP|`http://localhost:8080`|
|Продакшен|**Всегда** HTTPS|`https://myapp.com`|

---

## 4. Лабораторная работа: curl — рентген HTTP

Теория без практики — пустой звук. Давайте своими руками увидим всё, о чём говорили. Превратим теорию в **наблюдаемый факт**.

---

### 4.1. Что такое curl

**curl** — консольная утилита для отправки HTTP-запросов.

- Есть на любой ОС (Linux, macOS, Windows 10+)
- Позволяет увидеть **сырой текст HTTP** — без «красивостей» браузера
- Это «рентген»: показывает, что именно летит по проводу

Откройте терминал. Проверьте, что curl установлен:

Bash

```
curl --version
```

text

```
curl 8.x.x ...
```

Если видите версию — всё готово. Поехали.

---

### 4.2. Упражнение 1: Рентген обычного запроса

**Команда:**

Bash

```
curl -v http://example.com
```

Флаг `-v` (verbose) — «покажи мне ВСЁ».

**Что вы увидите** (лишнее убрано, оставлена суть):

text

```
* Connected to example.com (93.184.216.34) port 80
                                                       ┌─ МЫ отправили (Request)
> GET / HTTP/1.1                                       │  Стартовая строка
> Host: example.com                                    │  Заголовок: какой сайт
> User-Agent: curl/8.0.1                               │  Заголовок: кто спрашивает
> Accept: */*                                          │  Заголовок: приму любой формат
>                                                      ┘
                                                       ┌─ НАМ ответили (Response)
< HTTP/1.1 200 OK                                     │  Стартовая строка
< Content-Type: text/html                              │  Заголовок: формат ответа
< Content-Length: 1256                                 │  Заголовок: размер тела
<                                                      ┘

<!doctype html>                                        ← Тело ответа: HTML
<html>
<head>
    <title>Example Domain</title>
...
```

**Как читать:**

- Строки с `>` — то, что **мы** отправили (Request)
- Строки с `<` — то, что **нам** ответили (Response)

Разбираем ключевые строки:

text

```
> GET / HTTP/1.1
   │  │     │
   │  │     └─ Протокол и версия
   │  └─ Путь: корень сайта «/»
   └─ Метод: «Дай»

< HTTP/1.1 200 OK
      │     │   │
      │     │   └─ Текстовое пояснение
      │     └─ Код 200: успех 🟢
      └─ Протокол
```

**Вывод:** Вот он, HTTP — обычный текст. Строки, которые можно прочитать глазами. Никакой магии.

---

### 4.3. Упражнение 2: JSON API — то, с чем работает Java-backend

**Команда:**

Bash

```
curl -i https://httpbin.org/json
```

Флаг `-i` — показать заголовки ответа вместе с телом.

**Результат:**

text

```
HTTP/1.1 200 OK                              ← Код 200: успех 🟢
Content-Type: application/json                ← Сервер говорит: «Это JSON»

{
  "slideshow": {
    "author": "Yours Truly",
    "date": "date of publication",
    "slides": [
      {
        "title": "Wake up to WonderWidgets!",
        "type": "all"
      }
    ],
    "title": "Sample Slide Show"
  }
}
```

> 📝 **Примечание:** вы можете увидеть `HTTP/2 200` вместо `HTTP/1.1 200 OK`. Это нормально — `httpbin.org` поддерживает HTTP/2. Для нас разницы нет: те же коды, те же заголовки, тот же JSON.

**Что наблюдать:**

- `Content-Type: application/json` — сервер сообщает формат
- Тело — JSON-объект. Обычный. Читаемый. Текст.

**Связь с Java:** Ваш Spring Boot контроллер будет возвращать **именно такие** JSON-ответы. Метод с аннотацией `@GetMapping` вернёт объект, Spring сериализует его в JSON и отправит с заголовком `Content-Type: application/json`. Точно как в этом примере.

---

### 4.4. Упражнение 3: Редирект (302)

**Команда:**

Bash

```
curl -I http://httpbin.org/redirect/1
```

Флаг `-I` — показать **только** заголовки (HEAD-запрос, без тела).

**Результат:**

text

```
HTTP/1.1 302 FOUND                            ← Код 302: «Иди в другое место» 🟡
Location: /get                                ← Куда идти
```

Сервер говорит: «Того, что ты ищешь, здесь нет. Иди на `/get`».

**Автоматический переход:**

Bash

```
curl -L http://httpbin.org/redirect/1
```

Флаг `-L` (Location) — следовать за редиректами. curl перейдёт по адресу из заголовка `Location` и покажет финальный ответ.

---

### 4.5. Упражнение 4: Ошибка 404

**Команда:**

Bash

```
curl -i https://httpbin.org/status/404
```

**Результат:**

text

```
HTTP/1.1 404 NOT FOUND                        ← Код 404: ресурс не найден 🔴
```

**Что важно понять:**

- Сервер **ЖИВ** и **ОТВЕЧАЕТ** — просто ресурс не найден
- `404` — это **НЕ** «сервер сломался»
- `404` — это «ты постучался не в ту дверь»

**Сравните с `500`:**

Bash

```
curl -i https://httpbin.org/status/500
```

text

```
HTTP/1.1 500 INTERNAL SERVER ERROR            ← Код 500: сервер сломался 🔥
```

Вот **ЭТО** — «сервер сломался». Запрос нормальный, но на сервере что-то пошло не так.

---

### 4.6. Упражнение 5: POST-запрос с JSON-телом

**Команда:**

Bash

```
curl -v -X POST https://httpbin.org/post \
  -H "Content-Type: application/json" \
  -d '{"name": "Alice"}'
```

**Разбираем флаги:**

|Флаг|Что делает|
|---|---|
|`-v`|Verbose — покажи всё|
|`-X POST`|Используй метод POST|
|`-H "Content-Type: ..."`|Добавь заголовок|
|`-d '{"name": "Alice"}'`|Тело запроса (data)|

**Что вы увидите (запрос):**

text

```
> POST /post HTTP/1.1                         ← Метод POST (не GET!)
> Host: httpbin.org
> Content-Type: application/json               ← «Я шлю JSON»
> Content-Length: 16
>
```

> 📝 **Обратите внимание:** `curl -v` показывает заголовки запроса (строки с `>`), но **не отображает тело запроса** в этом потоке. Тело отправляется «молча». Но не волнуйтесь — `httpbin.org` работает как зеркало и покажет в ответе всё, что получил от вас.

**Что вы увидите (ответ):**

text

```
< HTTP/1.1 200 OK                             ← Сервер принял 🟢
< Content-Type: application/json
<
{
  "data": "{\"name\": \"Alice\"}",
  "headers": {
    "Content-Type": "application/json",
    "Host": "httpbin.org"
  },
  "json": {
    "name": "Alice"                            ← Вот что сервер получил от нас!
  },
  "url": "https://httpbin.org/post"
}
```

Поле `"json"` — это **эхо**. Сервер распарсил наш JSON и показал обратно. Всё дошло.

**Связь с Java:** Именно такие POST-запросы с JSON-телом будут прилетать в ваш `@PostMapping` контроллер. Клиент (фронтенд, мобильное приложение, другой сервис) отправит JSON, а ваш код его обработает.

---

## 5. Заключение: Собираем цепочку целиком

Всё, о чём мы говорили, — это звенья одной цепочки. Давайте соберём их вместе и увидим полную картину.

**Что происходит, когда вы вводите URL в браузер:**

text

```
[Вы вводите google.com]
        │
        ▼
[DNS: google.com → 142.250.74.46]       ← Телефонная книга
        │
        ▼
[TCP: соединение с 142.250.74.46:443]   ← Надёжная доставка
        │
        ▼
[HTTP Request: GET / HTTP/1.1]           ← Текстовый запрос
        │
        ▼
[Сервер обрабатывает]                    ← Java-приложение
        │
        ▼
[HTTP Response: 200 OK + HTML]           ← Текстовый ответ
        │
        ▼
[Браузер рисует HTML]                    ← Красивая картинка
```

**Формула для запоминания:**

text

```
DNS (имя → IP)
    → TCP (надёжное соединение)
        → HTTP (текстовый запрос-ответ)
            → Ваш Java-код (обработка)
                → Текстовый ответ клиенту
```

**Что это значит для вас как Java-разработчика:**

1. Вы пишете программу, которая **слушает порт** (обычно `8080`)
2. Получает **текстовый HTTP-запрос**
3. Разбирает: метод, путь, заголовки, тело
4. Выполняет логику (ходит в базу, считает, проверяет)
5. Возвращает **текстовый HTTP-ответ** с кодом статуса и телом (JSON/HTML)

Spring Boot, сервлеты, контроллеры, фильтры — это всё инструменты, которые делают шаги 2–5 удобнее. Но под капотом всегда одно и то же: **текстовый запрос прилетел → текстовый ответ улетел**.

> **Никакой магии. Только IP → TCP → HTTP → Текст → Ответ.**

---

## 📖 Словарь Сетевика

Краткие определения, к которым можно возвращаться:

|Термин|Определение|
|---|---|
|**IP-адрес**|Уникальный числовой адрес устройства в сети. Как почтовый адрес дома. Пример: `142.250.74.46`|
|**Порт**|Число от 0 до 65535, идентифицирующее конкретную программу на машине. Как номер квартиры в доме. Пример: `8080`|
|**DNS**|Domain Name System — распределённая система серверов, переводящая имя (`google.com`) в IP-адрес (`142.250.74.46`). Телефонная книга интернета|
|**localhost**|Специальное имя, всегда указывающее на ваш собственный компьютер. IP-эквивалент: `127.0.0.1`|
|**HTTP**|HyperText Transfer Protocol — текстовый протокол общения между клиентом и сервером по модели «запрос → ответ»|
|**HTTPS**|HTTP + TLS-шифрование. Те же текстовые сообщения, но в зашифрованном канале|
|**TCP**|Transmission Control Protocol — протокол надёжной доставки данных. Гарантирует, что всё дойдёт и в правильном порядке. HTTP работает поверх TCP|
|**GET**|HTTP-метод «Дай». Безопасный, не меняет данные на сервере|
|**POST**|HTTP-метод «Создай». Отправляет данные, меняет состояние на сервере|
|**JSON**|JavaScript Object Notation — текстовый формат для передачи данных. Выглядит как `{"key": "value"}`. Стандарт общения между фронтендом и бэкендом|
|**404**|Код ответа HTTP: «Ресурс не найден». Сервер жив, но по этому адресу ничего нет. Ошибка клиента (неверный URL)|
|**500**|Код ответа HTTP: «Внутренняя ошибка сервера». Запрос нормальный, но на сервере баг. Ваша вина как бэкенд-разработчика|
|**curl**|Консольная утилита для отправки HTTP-запросов. Позволяет увидеть сырой текст HTTP без браузера|

---

## 🛠 Практика: проверь себя

---

### Задание 1: Узнай IP Яндекса

**Задача:** узнайте IP-адрес сайта `yandex.ru` через терминал.

**Команда (скопируйте и вставьте):**

Bash

```
nslookup yandex.ru
```

**Что должны увидеть:**

text

```
Non-authoritative answer:
Name:   yandex.ru
Address: 5.255.255.242          ← IP-адрес Яндекса (у вас может отличаться)
```

**Проверьте себя:** откройте браузер и введите полученный IP прямо в адресную строку. Если открылся Яндекс — вы только что сделали работу DNS вручную. 🎉

**Бонус для Linux/macOS:**

Bash

```
dig yandex.ru +short
```

Эта команда выдаст **только** IP, без лишнего текста. Чисто и элегантно.

---

### Задание 2: Отправь POST-запрос как настоящий хакер

**Задача:** отправьте JSON-данные на тестовый сервер и получите зеркальный ответ.

**Команда (скопируйте и вставьте):**

Bash

```
curl -X POST https://httpbin.org/post \
  -H "Content-Type: application/json" \
  -d '{"hacker": "you", "skill": "networks", "level": "beginner"}'
```

**Что должны увидеть в ответе:**

JSON

```
{
  "json": {
    "hacker": "you",
    "skill": "networks",
    "level": "beginner"
  },
  "url": "https://httpbin.org/post"
}
```

Сервер вернул ваши данные обратно. Вы только что:

- ✅ Составили HTTP-запрос
- ✅ Использовали метод `POST`
- ✅ Отправили заголовок `Content-Type: application/json`
- ✅ Передали JSON в теле запроса
- ✅ Получили и прочитали ответ

**Это ровно то, что ваш будущий Java-контроллер будет получать от клиентов каждый день.**

**Бонус — измените данные и отправьте снова:**

Bash

```
curl -X POST https://httpbin.org/post \
  -H "Content-Type: application/json" \
  -d '{"name": "Ваше_имя", "message": "Привет, HTTP!"}'
```

Поменяйте значения. Добавьте новые поля. Поиграйте. Сломайте JSON (уберите кавычку) и посмотрите, что ответит сервер. **Ломать — лучший способ понять, как что-то работает.**

---

## Шпаргалка: все команды из статьи

Bash

```
# --- Адресация ---

# Узнать IP по имени
nslookup google.com
dig google.com

# --- HTTP через curl ---

# Рентген HTTP-запроса (показать ВСЁ)
curl -v http://example.com

# JSON API с заголовками ответа
curl -i https://httpbin.org/json

# Только заголовки (редирект)
curl -I http://httpbin.org/redirect/1

# Следовать за редиректом
curl -L http://httpbin.org/redirect/1

# Ошибка 404
curl -i https://httpbin.org/status/404

# Ошибка 500
curl -i https://httpbin.org/status/500

# POST с JSON-телом
curl -X POST https://httpbin.org/post \
  -H "Content-Type: application/json" \
  -d '{"name": "Alice"}'
```

---

> **Никакой магии. Только IP → TCP → HTTP → Текст → Ответ.**
> 
> Теперь вы знаете, что происходит за те 200 миллисекунд.